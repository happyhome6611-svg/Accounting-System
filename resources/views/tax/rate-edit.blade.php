@extends('layouts.app')
@section('content')
<h1>Edit Effective Tax Rate</h1><p class="text-muted">{{$company->entity_label}} · {{$code->code}}. Rates used by posted tax transactions are immutable.</p>
<form class="card p-4" method="post" action="{{route('tax.rates.update',[$company->country->code,$company,$code,$rate])}}">@csrf @method('PUT')<div class="row g-3"><div class="col-md-3"><label class="form-label">Rate %</label><input class="form-control" inputmode="decimal" name="rate" value="{{old('rate',$rate->rate)}}" required></div><div class="col-md-3"><label class="form-label">Effective From</label><input class="form-control" type="date" name="effective_from" value="{{old('effective_from',$rate->effective_from->toDateString())}}" required></div><div class="col-md-3"><label class="form-label">Effective To</label><input class="form-control" type="date" name="effective_to" value="{{old('effective_to',$rate->effective_to?->toDateString())}}"></div><div class="col-md-3 align-self-end"><label><input type="checkbox" name="is_active" value="1" @checked(old('is_active',$rate->is_active))> Active</label></div></div><div class="d-flex gap-2 mt-3"><button class="btn btn-primary">Update Rate</button><a class="btn btn-outline-secondary" href="{{route('tax.workspace',[$company->country->code,$company])}}">Cancel</a></div></form>
@endsection
