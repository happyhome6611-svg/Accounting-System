@extends('layouts.app') @section('content')
<h1>Purchases & Accounts Payable{{ $country ? ' — '.$country->name : '' }}</h1><form class="mb-3"><label class="form-label">Country / Jurisdiction</label><select class="form-select" name="country_id" onchange="this.form.submit()">@foreach($countries as $item)<option value="{{$item->id}}" @selected($country?->id===$item->id)>{{$item->name}}</option>@endforeach</select></form>
<div class="row g-3">@forelse($companies as $company)<div class="col-md-6"><div class="card p-4"><h4>{{$company->entity_label}}</h4><div class="d-flex flex-wrap gap-2"><a class="btn btn-primary" href="{{route('purchases.suppliers',$company)}}">Suppliers</a>@foreach(['orders'=>'Purchase Orders','bills'=>'Supplier Bills','credits'=>'Supplier Credits','payments'=>'Supplier Payments'] as $type=>$label)<a class="btn btn-primary" href="{{route('purchases.documents',[$company,$type])}}">{{$label}}</a>@endforeach<a class="btn btn-outline-primary" href="{{ route('purchases.reports', ['type' => 'ap', 'company_id' => $company->id]) }}">Accounts Payable</a><a class="btn btn-outline-primary" href="{{ route('purchases.reports', ['type' => 'aging', 'company_id' => $company->id]) }}">AP Aging</a></div></div></div>@empty<div class="alert alert-info">Purchases are available for Company and Sole Trader entities. Individual personal ledgers do not expose supplier workflows.</div>@endforelse</div>
@endsection
