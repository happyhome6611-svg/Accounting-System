@extends('layouts.app') @section('content')<h1>Suppliers — {{$company->entity_label}}</h1>@if(session('success'))<div class="alert alert-success">{{session('success')}}</div>@endif
<div class="card p-4 mb-4"><h2 class="h5">Add Supplier</h2><form method="post" action="{{route('purchases.suppliers.store',$company)}}" class="row g-3">@csrf @include('purchases.supplier-fields',['supplier'=>null])<div><button class="btn btn-primary">Add Supplier</button></div></form></div>
<div class="card table-responsive"><table class="table mb-0"><thead><tr><th>Code</th><th>Supplier</th><th>Status</th><th>Actions</th></tr></thead><tbody>@foreach($suppliers as $supplier)<tr><td>{{$supplier->code}}</td><td>{{$supplier->name}}</td><td>{{$supplier->is_active?'Active':'Inactive'}}</td><td class="d-flex gap-2"><a class="btn btn-sm btn-outline-primary" href="{{route('purchases.suppliers.edit',[$company,$supplier])}}">Edit</a><form method="post" action="{{route('purchases.suppliers.status',[$company,$supplier])}}">@csrf @method('PATCH')<input type="hidden" name="is_active" value="{{$supplier->is_active?0:1}}"><button class="btn btn-sm btn-outline-secondary">{{$supplier->is_active?'Deactivate':'Reactivate'}}</button></form>@if($deletable[$supplier->id])<form method="post" action="{{route('purchases.suppliers.destroy',[$company,$supplier])}}" onsubmit="return confirm('Permanently delete {{$supplier->name}}?')">@csrf @method('DELETE')<input type="hidden" name="confirmation_name" value="{{$supplier->name}}"><button class="btn btn-sm btn-outline-danger">Delete</button></form>@endif</td></tr>@endforeach</tbody></table></div>@endsection
