@extends('layouts.app')
@section('title', 'Import & Export Workspace')
@section('content')
<div class="d-flex justify-content-between align-items-start mb-4"><div><h1>Import & Export Workspace</h1><div class="text-muted">Jurisdiction: {{ $company->country->name }} · Entity: {{ $company->entity_label }} · {{ $company->supportsBranches() ? 'Branch-aware' : 'Branch: Not applicable' }}</div></div><div class="d-flex gap-2"><a class="btn btn-primary" href="{{ route('import-export.imports.create', [$company->country->code, $company]) }}">New Import</a><a class="btn btn-outline-primary" href="{{ route('import-export.exports.create', [$company->country->code, $company]) }}">New Export</a></div></div>
@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif
<div class="row g-4"><div class="col-lg-8"><div class="card"><div class="card-header fw-semibold">Import History</div><div class="table-responsive"><table class="table mb-0"><thead><tr><th>Date</th><th>File</th><th>Data Type</th><th>Status</th><th>Rows</th><th>Imported</th><th>Errors</th></tr></thead><tbody>@forelse($batches as $batch)<tr><td>{{ $batch->uploaded_at }}</td><td><a href="{{ route('import-export.batches.show', [$company->country->code, $company, $batch]) }}">{{ $batch->original_filename }}</a></td><td>{{ $importTypes[$batch->data_type] ?? $batch->data_type }}</td><td>{{ str($batch->status)->replace('_', ' ')->title() }}</td><td>{{ $batch->total_rows }}</td><td>{{ $batch->imported_rows }}</td><td>{{ $batch->invalid_rows + $batch->failed_rows }}</td></tr>@empty<tr><td colspan="7">No imports yet.</td></tr>@endforelse</tbody></table></div></div></div><div class="col-lg-4"><div class="card mb-4"><div class="card-header fw-semibold d-flex justify-content-between">Import Profiles <a href="{{ route('import-export.profiles.create', [$company->country->code, $company]) }}">Create</a></div><div class="list-group list-group-flush">@forelse($profiles as $profile)<a class="list-group-item list-group-item-action" href="{{ route('import-export.profiles.edit', [$company->country->code, $company, $profile]) }}">{{ $profile->name }}<small class="d-block text-muted">{{ $importTypes[$profile->data_type] ?? $profile->data_type }}</small></a>@empty<div class="list-group-item text-muted">No saved profiles.</div>@endforelse</div></div><div class="card"><div class="card-header fw-semibold">Export History</div><div class="list-group list-group-flush">@forelse($exports as $export)<div class="list-group-item">{{ $export->filename }}<small class="d-block text-muted">{{ $export->row_count }} rows · {{ $export->generated_at }}</small></div>@empty<div class="list-group-item text-muted">No exports yet.</div>@endforelse</div></div></div></div>
@endsection
