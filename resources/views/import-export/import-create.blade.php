@extends('layouts.app')
@section('title', 'New Import')
@section('content')
<nav aria-label="breadcrumb"><ol class="breadcrumb"><li class="breadcrumb-item"><a href="{{ route('import-export') }}">Import & Export</a></li><li class="breadcrumb-item"><a href="{{ route('import-export.country', $company->country->code) }}">{{ $company->country->name }}</a></li><li class="breadcrumb-item"><a href="{{ route('import-export.workspace', [$company->country->code, $company]) }}">{{ $company->entity_label }}</a></li><li class="breadcrumb-item active">New Import</li></ol></nav>
<h1>New Import</h1><p class="text-muted">{{ $company->country->name }} · {{ $company->entity_label }} · {{ str($company->entity_type)->replace('_', ' ')->title() }}. Files are staged and validated before records are created.</p>
<div class="alert alert-info">Workflow: Select type → Upload → XLSX worksheet (when required) → Map columns → Validate and preview → Confirm → Result.</div>
<div class="card card-body"><form method="post" enctype="multipart/form-data" action="{{ route('import-export.imports.upload', [$company->country->code, $company]) }}">@csrf<div class="row g-3"><div class="col-md-6"><label class="form-label" for="data_type">Import Type</label><select id="data_type" name="data_type" class="form-select" required><option value="">Choose an implemented import type</option>@foreach($types as $key=>$label)<option value="{{ $key }}" @selected(old('data_type', request('data_type')) === $key)>{{ $label }}</option>@endforeach</select></div>@if($company->supportsBranches())<div class="col-md-6"><label class="form-label" for="branch_id">Branch (transaction imports only)</label><select id="branch_id" name="branch_id" class="form-select"><option value="">Use mapped/default branch</option>@foreach($branches as $branch)<option value="{{ $branch->id }}">{{ $branch->code }} — {{ $branch->name }}</option>@endforeach</select><div class="form-text">Company and Sole Trader transactions may use a mapped branch.</div></div>@else<div class="col-md-6"><label class="form-label">Branch</label><input class="form-control" value="Not applicable for Individual" disabled></div>@endif<div class="col-12"><label class="form-label" for="file">Upload File</label><input id="file" type="file" name="file" accept=".csv,.xlsx,text/csv,application/vnd.openxmlformats-officedocument.spreadsheetml.sheet" class="form-control" required><div class="form-text">Supported formats: CSV / XLSX. Maximum {{ config('imports.max_file_kb') / 1024 }} MB and {{ number_format(config('imports.max_rows')) }} rows. Formulas, macros and external links are never executed.</div></div><div class="col-12 d-flex gap-2"><button class="btn btn-primary">Upload / Continue</button><a class="btn btn-outline-secondary" href="{{ route('import-export.workspace', [$company->country->code, $company]) }}">Cancel</a></div></div></form></div>
@endsection
