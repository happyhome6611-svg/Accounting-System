@extends('layouts.app')
@section('title', 'New Import')
@section('content')
<h1>New Import</h1><p class="text-muted">{{ $company->country->name }} · {{ $company->entity_label }}. Files are staged and validated before any production record is created.</p>
<div class="card card-body"><form method="post" enctype="multipart/form-data" action="{{ route('import-export.imports.upload', [$company->country->code, $company]) }}">@csrf<div class="row g-3"><div class="col-md-6"><label class="form-label">Data Type</label><select name="data_type" class="form-select" required>@foreach($types as $key=>$label)<option value="{{ $key }}">{{ $label }}</option>@endforeach</select></div>@if($company->supportsBranches())<div class="col-md-6"><label class="form-label">Branch (transactions only)</label><select name="branch_id" class="form-select"><option value="">Use mapped/default branch</option>@foreach($branches as $branch)<option value="{{ $branch->id }}">{{ $branch->name }}</option>@endforeach</select></div>@endif<div class="col-12"><label class="form-label">CSV or XLSX File</label><input type="file" name="file" accept=".csv,.xlsx" class="form-control" required><div class="form-text">Maximum {{ config('imports.max_file_kb') / 1024 }} MB and {{ number_format(config('imports.max_rows')) }} rows. Formulas, macros and external links are never executed.</div></div><div class="col-12"><button class="btn btn-primary">Upload and Inspect</button></div></div></form></div>
@endsection
