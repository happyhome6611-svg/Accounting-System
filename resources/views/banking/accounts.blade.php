@extends('layouts.app') @section('content')<h1>Bank & Cash Accounts — {{$company->entity_label}}</h1>@if(session('success'))<div class="alert alert-success">{{session('success')}}</div>@endif<div class="card p-3 mb-3"><form method="post" action="{{route('banking.accounts.store',$company)}}">@csrf<div class="row g-2"><div class="col-md-3"><label class="form-label">Account Name</label><input class="form-control" name="name" required></div><div class="col-md-2"><label class="form-label">Type</label><select class="form-select" name="type"><option value="bank">Bank</option><option value="cash">Cash</option></select></div><div class="col-md-3"><label class="form-label">Linked Ledger Account</label><select class="form-select" name="ledger_account_id">@foreach($ledgerAccounts as $ledger)<option value="{{$ledger->id}}">{{$ledger->code}} — {{$ledger->name}}</option>@endforeach</select></div><div class="col-md-2"><label class="form-label">Bank Name</label><input class="form-control" name="bank_name"></div><input type="hidden" name="currency_id" value="{{$company->base_currency_id}}"><div class="col-md-2 align-self-end"><button class="btn btn-primary">Add Account</button></div></div></form></div><div class="card table-responsive"><table class="table mb-0"><thead><tr><th>Name</th><th>Type</th><th>Ledger</th><th>Currency</th><th>Status</th><th>Actions</th></tr></thead><tbody>@foreach($accounts as $account)<tr><td>{{$account->name}}</td><td>{{ucfirst($account->type)}}</td><td>{{$account->ledgerAccount->code}} — {{$account->ledgerAccount->name}}</td><td>{{$company->baseCurrency->code}}</td><td>{{$account->is_active?'Active':'Inactive'}}</td><td class="d-flex flex-wrap gap-1"><a class="btn btn-sm btn-outline-primary" href="{{route('banking.register',[$company,$account])}}">View Register</a><a class="btn btn-sm btn-outline-primary" href="{{route('banking.imports',[$company,$account])}}">Import Statement</a><a class="btn btn-sm btn-outline-primary" href="{{route('banking.matching',[$company,$account])}}">Matching</a><a class="btn btn-sm btn-outline-primary" href="{{route('banking.reconciliations',[$company,$account])}}">Reconcile</a></td></tr>@endforeach</tbody></table></div><a class="btn btn-primary mt-3" href="{{route('banking.transactions.create',$company)}}">New Banking Transaction</a>@endsection
