@extends('layouts.app') @section('content')<h1>Import Preview</h1><table class="table">@foreach($lines as $line)<tr>@foreach(str_getcsv($line) as $value)<td>{{$value}}</td>@endforeach</tr>@endforeach</table><form method="post" action="{{route('banking.imports.confirm',[$company,$account])}}">@csrf<label><input type="checkbox" name="override" value="1"> Allow confirmed duplicates</label><button class="btn btn-primary ms-3">Confirm Import</button></form>@endsection
