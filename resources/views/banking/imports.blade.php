@extends('layouts.app') @section('content')<h1>Statement Imports — {{$account->name}}</h1>@if(session('success'))<div class="alert alert-success">{{session('success')}}</div>@endif<div class="alert alert-info">Upload → Preview → Confirm Import. Imported rows do not create journals.</div><form method="post" enctype="multipart/form-data" action="{{route('banking.imports.preview',[$company->country,$company,$account])}}" class="card p-4">@csrf<label class="form-label">CSV Statement</label><input class="form-control mb-3" type="file" name="statement" accept=".csv,text/csv" required><button class="btn btn-primary">Preview</button></form>@endsection
