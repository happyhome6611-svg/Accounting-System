@extends('layouts.app')
@section('content')
<div class="row justify-content-center"><div class="col-lg-7"><div class="card border border-danger p-4"><h1 class="text-danger">Permanently Delete Company</h1><p class="lead">{{ $company->name }}</p><div class="alert alert-danger"><strong>This action permanently deletes the unused company and cannot be undone.</strong></div><p>Its generated financial year, accounting periods, chart of accounts, and unused document sequences will also be removed.</p><form method="post" action="{{ route('companies.destroy',$company) }}">@csrf @method('DELETE')<label class="form-label" for="confirmation_name">Type <strong>{{ $company->name }}</strong> to confirm</label><input class="form-control" id="confirmation_name" name="confirmation_name" autocomplete="off" required><div class="d-flex gap-2 mt-4"><a class="btn btn-secondary" href="{{ route('companies.index') }}">Cancel</a><button class="btn btn-danger">Permanently Delete Company</button></div></form></div></div></div>
@endsection
