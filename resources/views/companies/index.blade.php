@extends('layouts.app')
@section('content')
<nav aria-label="breadcrumb"><ol class="breadcrumb"><li class="breadcrumb-item"><a href="{{ route('companies.index') }}">Accounting Entities</a></li><li class="breadcrumb-item active">{{ $country->name }}</li></ol></nav>
<div class="d-flex justify-content-between align-items-center"><h1>Accounting Entities — {{ $country->name }}</h1><a class="btn btn-primary" href="{{ route('companies.country.create', $country->code) }}">New entity</a></div>
@if(session('success'))<div class="alert alert-success mt-3">{{ session('success') }}</div>@endif
<div class="row mt-3 g-3">@forelse($companies as $company)<div class="col-lg-4"><div class="card p-4 h-100"><h4><a class="text-decoration-none" href="{{ route('companies.show',$company) }}">{{ $company->name }}</a></h4><div>{{ $company->country->name }} · {{ $company->baseCurrency->code }}</div><span class="badge mt-2 {{ $company->is_active?'bg-success':'bg-secondary' }}">{{ $company->is_active?'Active':'Inactive' }}</span><div class="border-top mt-4 pt-3 d-flex flex-wrap gap-2"><a class="btn btn-sm btn-outline-primary" href="{{ route('companies.edit',$company) }}">Edit</a><a class="btn btn-sm btn-outline-primary" href="{{ route('companies.financial-years.index', $company) }}">Manage Financial Years</a>@if($company->supportsBranches())<a class="btn btn-sm btn-outline-primary" href="{{ route('companies.branches.index', $company) }}">Manage Branches</a>@endif<form method="post" action="{{ route('companies.status',$company) }}">@csrf @method('PATCH')<input type="hidden" name="is_active" value="{{ $company->is_active?0:1 }}"><button class="btn btn-sm btn-outline-secondary">{{ $company->is_active?'Deactivate':'Reactivate' }}</button></form>@if($deletable[$company->id])<a class="btn btn-sm btn-outline-danger" href="{{ route('companies.delete',$company) }}">Delete</a>@endif</div></div></div>@empty<p>No accounting entities yet.</p>@endforelse</div>
<div class="mt-4"><a href="{{ route('companies.index') }}">← Back to Accounting Jurisdictions</a></div>
@endsection
