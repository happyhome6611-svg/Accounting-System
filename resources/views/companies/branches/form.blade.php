@extends('layouts.app')
@section('content')
<h1>{{ $branch->exists ? 'Edit Branch' : 'Add Branch' }}</h1><p class="text-muted">{{ $company->name }}</p>
<form class="card p-4" method="post" action="{{ $branch->exists ? route('companies.branches.update', [$company, $branch]) : route('companies.branches.store', $company) }}">@csrf @if($branch->exists) @method('PUT') @endif
<div class="row g-3"><div class="col-md-4"><label class="form-label">Branch Code</label><input class="form-control" name="code" required value="{{ old('code', $branch->code) }}"></div><div class="col-md-8"><label class="form-label">Branch Name</label><input class="form-control" name="name" required value="{{ old('name', $branch->name) }}"></div><div class="col-md-6"><label class="form-label">Legal Name</label><input class="form-control" name="legal_name" value="{{ old('legal_name', $branch->legal_name) }}"></div><div class="col-md-6"><label class="form-label">Timezone</label><input class="form-control" name="timezone" value="{{ old('timezone', $branch->timezone ?: $company->timezone) }}"></div><div class="col-md-6"><label class="form-label">Email</label><input class="form-control" type="email" name="email" value="{{ old('email', $branch->email) }}"></div><div class="col-md-6"><label class="form-label">Phone</label><input class="form-control" name="phone" value="{{ old('phone', $branch->phone) }}"></div><div class="col-12"><label class="form-label">Address</label><textarea class="form-control" name="address">{{ old('address', $branch->address) }}</textarea></div></div><div class="d-flex gap-2 mt-4"><button class="btn btn-primary">Save Branch</button><a class="btn btn-outline-secondary" href="{{ route('companies.branches.index', $company) }}">Cancel</a></div></form>
@endsection
