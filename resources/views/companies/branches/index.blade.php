@extends('layouts.app')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-3"><div><h1 class="mb-0">Branches</h1><div class="text-muted">{{ $company->name }}</div></div><a class="btn btn-primary" href="{{ route('companies.branches.create', $company) }}">Add Branch</a></div>
<div class="card"><div class="table-responsive"><table class="table mb-0"><thead><tr><th>Code</th><th>Name</th><th>Status</th><th>Type</th><th class="text-end">Actions</th></tr></thead><tbody>
@foreach($branches as $branch)<tr><td>{{ $branch->code }}</td><td>{{ $branch->name }}</td><td>{{ $branch->is_active ? 'Active' : 'Inactive' }}</td><td>{{ $branch->is_main_branch ? 'Main branch' : 'Branch' }}</td><td class="text-end"><a class="btn btn-sm btn-outline-primary" href="{{ route('companies.branches.edit', [$company, $branch]) }}">Edit</a> <form class="d-inline" method="post" action="{{ route('companies.branches.status', [$company, $branch]) }}">@csrf @method('PATCH')<input type="hidden" name="is_active" value="{{ $branch->is_active ? 0 : 1 }}"><button class="btn btn-sm btn-outline-secondary">{{ $branch->is_active ? 'Deactivate' : 'Reactivate' }}</button></form>@if(!$branch->is_main_branch && !$service->blockers($branch)) <form class="d-inline" method="post" action="{{ route('companies.branches.destroy', [$company, $branch]) }}" onsubmit="return confirm('Permanently delete this unused branch?')">@csrf @method('DELETE')<input type="hidden" name="confirmation_name" value="{{ $branch->name }}"><button class="btn btn-sm btn-outline-danger">Delete</button></form>@endif</td></tr>@endforeach
</tbody></table></div></div>
@endsection
