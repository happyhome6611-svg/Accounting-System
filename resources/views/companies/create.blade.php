@extends('layouts.app') @section('content')<h1>Create company</h1><div class="card p-4"><form method="post" action="{{ route('companies.store') }}">@csrf<div class="row">@foreach(['name'=>'Company name','legal_name'=>'Legal name','timezone'=>'Timezone'] as $name=>$label)<div class="col-md-6 mb-3"><label class="form-label">{{ $label }}</label><input class="form-control" name="{{ $name }}" value="{{ old($name,$name==='timezone'?'Pacific/Auckland':'') }}" required></div>@endforeach<div class="col-md-6 mb-3"><label class="form-label">Country</label><select class="form-select" name="country_id" required>@foreach($countries as $country)<option value="{{ $country->id }}">{{ $country->name }}</option>@endforeach</select></div><div class="col-md-6 mb-3"><label class="form-label">Base currency</label><select class="form-select" name="base_currency_id" required>@foreach($currencies as $currency)<option value="{{ $currency->id }}">{{ $currency->code }} — {{ $currency->name }}</option>@endforeach</select></div><div class="col-md-3 mb-3"><label class="form-label">Financial year starts</label><input class="form-control" type="date" name="financial_year_start" required></div><div class="col-md-3 mb-3"><label class="form-label">Financial year ends</label><input class="form-control" type="date" name="financial_year_end" required></div></div><button class="btn btn-primary">Create company</button></form></div>@endsection
