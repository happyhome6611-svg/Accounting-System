@extends('layouts.app')
@section('content')
<div class="row justify-content-center"><div class="col-lg-7"><div class="card border border-danger p-4"><h1 class="text-danger">Permanently Delete Customer</h1><p class="lead">{{ $customer->name }}</p>@if($blockers)<div class="alert alert-warning">This customer cannot be deleted because it is referenced by {{ implode(', ',$blockers) }}. Deactivate the customer instead.</div><a class="btn btn-secondary" href="{{ route('sales.customers',$company) }}">Back</a>@else<div class="alert alert-danger"><strong>This action permanently deletes the unused customer and cannot be undone.</strong></div><form method="post" action="{{ route('sales.customers.destroy',[$company,$customer]) }}">@csrf @method('DELETE')<label class="form-label">Type <strong>{{ $customer->name }}</strong> to confirm</label><input class="form-control" name="confirmation_name" required autocomplete="off"><div class="mt-3"><a class="btn btn-secondary" href="{{ route('sales.customers',$company) }}">Cancel</a><button class="btn btn-danger">Permanently Delete Customer</button></div></form>@endif</div></div></div>
@endsection
