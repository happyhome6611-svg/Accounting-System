@extends('layouts.app') @section('content')<div class="card border-danger p-4"><h1>Delete {{ $item->name }}</h1>@if($blockers)<div class="alert alert-warning">Referenced by {{ implode(', ',$blockers) }}. Deactivate instead.</div>@else<p>This permanently deletes the unused item and cannot be undone.</p><form method="post" action="{{ route('sales.items.destroy',[$company,$item]) }}">@csrf @method('DELETE')<label>Type {{ $item->name }} to confirm</label><input class="form-control" name="confirmation_name"><button class="btn btn-danger mt-3">Delete Permanently</button></form>@endif</div>@endsection
