@extends('layouts.app')
@section('content')
<div class="d-flex justify-content-between align-items-center mb-3"><div><h1 class="mb-0">{{ $title }}</h1><div class="text-muted">{{ $company->name }}</div></div><a class="btn btn-primary" href="{{ route('sales.transactions.create', [$company, $type]) }}">New {{ $singular }}</a></div>
<div class="card"><div class="table-responsive"><table class="table align-middle mb-0"><thead><tr><th>Number</th><th>Customer</th><th>Branch</th><th>Date</th>@if($type==='quotations')<th>Expiry</th>@elseif($type==='invoices')<th>Due</th>@endif<th class="text-end">Total</th>@if($type==='invoices')<th class="text-end">Outstanding</th>@endif<th>Status</th><th>Actions</th></tr></thead><tbody>@forelse($documents as $document)<tr><td>{{ $document->{$number} }}</td><td>{{ $document->customer->name }}</td><td>{{ $document->branch->name }}</td><td>{{ $document->{$date}->format('d M Y') }}</td>@if($type==='quotations')<td>{{ $document->expiry_date?->format('d M Y') }}</td>@elseif($type==='invoices')<td>{{ $document->due_date->format('d M Y') }}</td>@endif<td class="text-end">{{ $money->format($document->total ?? $document->amount, $company->baseCurrency) }}</td>@if($type==='invoices')<td class="text-end">{{ $money->format($document->amount_due, $company->baseCurrency) }}</td>@endif<td>{{ ucwords(str_replace('_',' ',$document->status)) }}</td><td><div class="d-flex gap-1"><a class="btn btn-sm btn-outline-primary" href="{{ route('sales.transactions.show', [$company,$type,$document]) }}">View</a>@if($document->status==='draft')<a class="btn btn-sm btn-outline-secondary" href="{{ route('sales.transactions.edit', [$company,$type,$document]) }}">Edit</a>@endif</div></td></tr>@empty<tr><td colspan="10" class="text-muted">No documents yet.</td></tr>@endforelse</tbody></table></div></div>
@endsection
