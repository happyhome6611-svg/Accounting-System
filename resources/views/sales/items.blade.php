@extends('layouts.app') @section('content')<h1>Products & Services — {{ $company->name }}</h1><div class="card p-4"><h2 class="h5">Add Product or Service</h2><form method="post" class="row g-3">@csrf<div class="col-md-4"><label class="form-label">Item Code</label><input class="form-control" name="code" required></div><div class="col-md-8"><label class="form-label">Item Name</label><input class="form-control" name="name" required></div><div class="col-md-3"><label class="form-label">Type</label><select class="form-select" name="type"><option value="service">Service</option><option value="product">Product</option></select></div><div class="col-md-3"><label class="form-label">Unit</label><input class="form-control" name="unit" value="each"></div><div class="col-md-3"><label class="form-label">Sales Price ({{ $money->symbol($company->baseCurrency) }} / {{ $company->baseCurrency->code }})</label><input type="number" step=".01" min="0" class="form-control" name="sales_price" value="0.00"></div><div class="col-md-3"><label class="form-label">Revenue Account</label><select class="form-select" name="revenue_account_id">@foreach($company->accounts()->where('type','revenue')->get() as $a)<option value="{{ $a->id }}">{{ $a->code }} — {{ $a->name }}</option>@endforeach</select></div><div><button class="btn btn-primary">Add Item</button></div></form></div><div class="table-responsive card mt-3"><table class="table table-hover mb-0"><tr><th>Code</th><th>Name</th><th>Type</th><th class="text-end">Price</th><th>Status</th><th>Actions</th></tr>@foreach($items as $item)<tr><td>{{ $item->code }}</td><td>{{ $item->name }}</td><td>{{ ucfirst($item->type) }}</td><td class="text-end">{{ $money->format($item->sales_price,$company->baseCurrency) }}</td><td>{{ $item->is_active?'Active':'Inactive' }}</td><td><div class="d-flex gap-2"><a class="btn btn-sm btn-outline-primary" href="{{ route('sales.items.edit',[$company,$item]) }}">Edit</a><form method="post" action="{{ route('sales.items.status',[$company,$item]) }}">@csrf @method('PATCH')<input type="hidden" name="is_active" value="{{ $item->is_active?0:1 }}"><button class="btn btn-sm btn-outline-secondary">{{ $item->is_active?'Deactivate':'Reactivate' }}</button></form>@if($deletable[$item->id])<a class="btn btn-sm btn-outline-danger" href="{{ route('sales.items.delete',[$company,$item]) }}">Delete</a>@endif</div></td></tr>@endforeach</table></div>@endsection
