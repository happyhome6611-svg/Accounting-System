@extends('layouts.app') @section('content')<h1>Customers — {{$company->name}}</h1><div class="card p-3 mb-3"><form method="post" class="row g-2">@csrf<input class="form-control col" name="code" placeholder="Code (auto if blank)"><input class="form-control col" name="name" placeholder="Customer name" required><input type="email" class="form-control col" name="email" placeholder="Email"><input type="hidden" name="currency_id" value="{{$company->base_currency_id}}"><select class="form-select col" name="receivable_account_id">@foreach($company->accounts()->where('type','asset')->get() as $a)<option value="{{$a->id}}">{{$a->code}} {{$a->name}}</option>@endforeach</select><input type="number" name="payment_terms_days" value="30" class="form-control col"><input type="number" name="credit_limit" value="0" step=".0001" class="form-control col"><button class="btn btn-primary col">Add Customer</button></form></div><table class="table"><tr><th>Code</th><th>Name</th><th>Email</th><th>Status</th></tr>@foreach($customers as $c)<tr><td>{{$c->code}}</td><td>{{$c->name}}</td><td>{{$c->email}}</td><td>{{$c->is_active?'Active':'Inactive'}}</td></tr>@endforeach</table>@endsection
