@extends('layouts.app') @section('content')<h1>Edit Product / Service</h1><div class="card p-4"><form method="post" action="{{ route('sales.items.update',[$company,$item]) }}" class="row g-3">@csrf @method('PUT')<div class="col-md-6"><label>Item Code</label><input class="form-control" name="code" value="{{ $item->code }}"></div><div class="col-md-6"><label>Item Name</label><input class="form-control" name="name" value="{{ $item->name }}"></div><div class="col-md-4"><label>Type</label><select class="form-select" name="type"><option value="service" @selected($item->type==='service')>Service</option><option value="product" @selected($item->type==='product')>Product</option></select></div><div class="col-md-4"><label>Unit</label><input class="form-control" name="unit" value="{{ $item->unit }}"></div><div class="col-md-4"><label>Sales Price ({{ $money->symbol($company->baseCurrency) }} / {{ $company->baseCurrency->code }})</label><input class="form-control" type="number" step=".01" name="sales_price" value="{{ $item->sales_price }}"></div><div class="col-md-6"><label>Revenue Account</label><select class="form-select" name="revenue_account_id">@foreach($company->accounts()->where('type','revenue')->get() as $a)<option value="{{ $a->id }}" @selected($item->revenue_account_id===$a->id)>{{ $a->code }} — {{ $a->name }}</option>@endforeach</select></div><input type="hidden" name="description" value="{{ $item->description }}"><div><a class="btn btn-secondary" href="{{ route('sales.items',$company) }}">Cancel</a><button class="btn btn-primary">Save</button></div></form></div>@endsection
