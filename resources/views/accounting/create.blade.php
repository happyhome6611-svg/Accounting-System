@extends('layouts.app')
@section('content')
<h1>Create Journal — {{ $company->name }}</h1>
<form method="post" action="{{ route('journals.store', $company) }}" class="card p-4">@csrf<input type="hidden" name="company_id" value="{{ $company->id }}">
<div class="row g-3"><div class="col-md-4"><label class="form-label">Branch</label><select name="branch_id" class="form-select"><option value="">Select branch</option>@foreach($branches as $branch)<option value="{{ $branch->id }}" @selected($branches->count() === 1)>{{ $branch->code }} — {{ $branch->name }}</option>@endforeach</select></div><div class="col-md-4"><label class="form-label">Financial Year</label><select name="financial_year_id" class="form-select">@foreach($years as $year)<option value="{{ $year->id }}">{{ $year->name }}</option>@endforeach</select></div><div class="col-md-4"><label class="form-label">Period</label><select name="accounting_period_id" class="form-select">@foreach($years as $year) @foreach($year->periods as $period)<option value="{{ $period->id }}">{{ $period->name }} ({{ $period->status }})</option>@endforeach @endforeach</select></div><div class="col-md-4"><label class="form-label">Date</label><input type="date" name="transaction_date" class="form-control" required></div><div class="col-md-4"><label class="form-label">Reference</label><input name="reference" class="form-control"></div><div class="col-md-8"><label class="form-label">Description</label><input name="description" class="form-control" required></div></div>
<h5 class="mt-4">Lines</h5><div id="lines">@foreach([0, 1] as $lineIndex)<div class="row g-2 mb-2 line"><div class="col-4"><select name="lines[{{ $lineIndex }}][account_id]" class="form-select">@foreach($accounts as $account)<option value="{{ $account->id }}">{{ $account->code }} — {{ $account->name }}</option>@endforeach</select></div><div class="col-4"><input name="lines[{{ $lineIndex }}][description]" class="form-control" placeholder="Description"></div><div class="col-2"><input name="lines[{{ $lineIndex }}][debit]" type="number" min="0" step="0.0001" value="0" class="form-control" aria-label="Debit"></div><div class="col-2"><input name="lines[{{ $lineIndex }}][credit]" type="number" min="0" step="0.0001" value="0" class="form-control" aria-label="Credit"></div></div>@endforeach</div><button type="button" class="btn btn-outline-secondary align-self-start" onclick="addLine()">Add Line</button><button class="btn btn-primary mt-3 align-self-start">Save Draft</button></form>
<script>let n=2;function addLine(){let e=document.querySelector('.line').cloneNode(true);e.querySelectorAll('select,input').forEach(x=>{x.name=x.name.replace(/\[0\]/,'['+(n)+']');if(x.tagName==='INPUT')x.value=x.type==='number'?'0':''});document.querySelector('#lines').append(e);n++}</script>
@endsection
